#include <bits/stdc++.h>
#include "Project.h"
const int MAX=150;
using namespace std;
int main(){
    char* full;
    char* command;
    char* argument;
    do{
    } while (strcmp(command, "exit"));
}